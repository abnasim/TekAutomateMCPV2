# Legacy Provider Prompt

This split SCPI-specific prompt is now legacy.

Use the unified canonical prompt instead:

- [PROVIDER_MANIFEST_PROMPT.md](C:/Users/u650455/Desktop/Tek_Automator/Tek_Automator/mcp-server/PROVIDER_MANIFEST_PROMPT.md)

That prompt decides whether the source should become:

- a `flow_template`
- an overlay such as `static_result`
- or both, when the source genuinely supports both
